{
  "name": "challenge-8-vehicle-builder",
  "version": "1.0.0",
  "description": "A CLI application to create, manage, and interact with vehicles like cars, trucks, and motorbikes.",
  "main": "dist/index.js",
  "type": "module",
  "scripts": {
    "build": "npx tsc",
    "start": "npm run build && node dist/index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean": "rm -rf dist"
  },
  "author": "Alam",
  "license": "MIT",
  "dependencies": {
    "inquirer": "^9.2.19",
    "typescript": "^5.4.5"
  },
  "devDependencies": {
    "@types/inquirer": "^9.0.7",
    "@types/node": "^20.12.7"
  }
}
